meta {
  name: 09 List Project Timelogs
  type: http
  seq: 9
}

get {
  url: {{baseUrl}}/projects/{{projectId}}/timelogs
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{accessToken}}
}

tests {
  test("list project timelogs returns 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
