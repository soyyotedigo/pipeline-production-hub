meta {
  name: 08 Delete Timelog
  type: http
  seq: 12
}

delete {
  url: {{baseUrl}}/timelogs/{{timelogId}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{accessToken}}
}

tests {
  test("delete timelog returns 204", function() {
    expect(res.getStatus()).to.equal(204);
  });
}

