meta {
  name: 07 Get Timelog
  type: http
  seq: 7
}

get {
  url: {{baseUrl}}/timelogs/{{timelogId}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{accessToken}}
}

tests {
  test("get timelog returns 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
