meta {
  name: 08 List Project Episodes
  type: http
  seq: 8
}

get {
  url: {{baseUrl}}/projects/{{projectId}}/episodes
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{accessToken}}
}

tests {
  test("list project episodes returns 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
