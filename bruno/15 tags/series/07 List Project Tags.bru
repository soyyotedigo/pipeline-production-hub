meta {
  name: 07 List Project Tags
  type: http
  seq: 7
}

get {
  url: {{baseUrl}}/projects/{{projectId}}/tags
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{accessToken}}
}

tests {
  test("list project tags returns 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
