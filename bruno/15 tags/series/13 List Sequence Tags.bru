meta {
  name: 13 List Sequence Tags
  type: http
  seq: 13
}

get {
  url: {{baseUrl}}/sequences/{{sequenceId}}/tags
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{accessToken}}
}

tests {
  test("list sequence tags returns 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
