meta {
  name: 07 List Project Notes
  type: http
  seq: 7
}

get {
  url: {{baseUrl}}/projects/{{projectId}}/notes
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{accessToken}}
}

tests {
  test("list project notes returns 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
