meta {
  name: 15 Get Playlist
  type: http
  seq: 15
}

get {
  url: {{baseUrl}}/playlists/{{playlistId}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{accessToken}}
}

tests {
  test("get playlist returns 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
