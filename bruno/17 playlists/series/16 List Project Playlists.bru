meta {
  name: 16 List Project Playlists
  type: http
  seq: 16
}

get {
  url: {{baseUrl}}/projects/{{projectId}}/playlists
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{accessToken}}
}

tests {
  test("list project playlists returns 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
