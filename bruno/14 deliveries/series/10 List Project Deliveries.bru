meta {
  name: 10 List Project Deliveries
  type: http
  seq: 10
}

get {
  url: {{baseUrl}}/projects/{{projectId}}/deliveries
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{accessToken}}
}

tests {
  test("list project deliveries returns 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
