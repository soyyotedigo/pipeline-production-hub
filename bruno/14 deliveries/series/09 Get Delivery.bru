meta {
  name: 09 Get Delivery
  type: http
  seq: 9
}

get {
  url: {{baseUrl}}/deliveries/{{deliveryId}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{accessToken}}
}

tests {
  test("get delivery returns 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
