meta {
  name: 13 List Delivery Items
  type: http
  seq: 13
}

get {
  url: {{baseUrl}}/deliveries/{{deliveryId}}/items
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{accessToken}}
}

tests {
  test("list delivery items returns 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
