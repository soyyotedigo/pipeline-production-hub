meta {
  name: 06 Assign Role
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/users/{{newUserId}}/roles
  body: json
  auth: none
}

headers {
  content-type: application/json
  Authorization: Bearer {{accessToken}}
}

body:json {
  {
    "role_name": "artist"
  }
}

tests {
  test("assign role returns 201", function() {
    expect(res.getStatus()).to.equal(201);
  });
}
