meta {
  name: 09 Deactivate User
  type: http
  seq: 9
}

post {
  url: {{baseUrl}}/users/{{newUserId}}/deactivate
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{accessToken}}
}

tests {
  test("deactivate user returns 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
