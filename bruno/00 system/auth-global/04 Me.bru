meta {
  name: 04 Me
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/auth/me
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{accessToken}}
}

tests {
  test("me returns 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
}
