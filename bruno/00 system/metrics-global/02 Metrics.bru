meta {
  name: 02 Metrics
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/metrics
  body: none
  auth: none
}

tests {
  test("metrics returns 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
