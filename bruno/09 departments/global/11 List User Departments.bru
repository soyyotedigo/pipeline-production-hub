meta {
  name: 11 List User Departments
  type: http
  seq: 11
}

get {
  url: {{baseUrl}}/users/{{departmentUserId}}/departments
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{accessToken}}
}

tests {
  test("list user departments returns 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
