meta {
  name: 05 Get Department
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/departments/{{departmentId}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{accessToken}}
}

tests {
  test("get department returns 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
