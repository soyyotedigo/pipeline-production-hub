meta {
  name: 06 List Departments
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/departments
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{accessToken}}
}

tests {
  test("list departments returns 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
