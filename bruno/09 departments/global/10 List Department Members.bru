meta {
  name: 10 List Department Members
  type: http
  seq: 10
}

get {
  url: {{baseUrl}}/departments/{{departmentId}}/members
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{accessToken}}
}

tests {
  test("list department members returns 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
