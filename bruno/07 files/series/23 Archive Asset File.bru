meta {
  name: 23 Archive Asset File
  type: http
  seq: 23
}

post {
  url: {{baseUrl}}/files/{{assetFileId}}/archive
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{accessToken}}
}

tests {
  test("archive asset file returns 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
