meta {
  name: 09 Scaffold Project
  type: http
  seq: 9
}

post {
  url: {{baseUrl}}/projects/{{projectId}}/scaffold
  body: json
  auth: none
}

headers {
  content-type: application/json
  Authorization: Bearer {{accessToken}}
}

body:json {
  {}
}

tests {
  test("scaffold returns 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
