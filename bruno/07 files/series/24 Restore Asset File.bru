meta {
  name: 24 Restore Asset File
  type: http
  seq: 24
}

post {
  url: {{baseUrl}}/files/{{assetFileId}}/restore
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{accessToken}}
}

tests {
  test("restore asset file returns 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
