meta {
  name: 08 Delete Created Shot
  type: http
  seq: 8
}

delete {
  url: {{baseUrl}}/shots/{{shotId}}
  body: none
  auth: none
}

params:query {
  force: True
}

headers {
  Authorization: Bearer {{accessToken}}
}

tests {
  test("delete shot returns 204", function() {
    expect(res.getStatus()).to.equal(204);
  });
}
