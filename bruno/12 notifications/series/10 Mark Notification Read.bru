meta {
  name: 10 Mark Notification Read
  type: http
  seq: 10
}

patch {
  url: {{baseUrl}}/notifications/{{notificationId}}/read
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{notificationUserAccessToken}}
}

tests {
  test("mark notification read returns 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
