meta {
  name: 09 Get Unread Count
  type: http
  seq: 9
}

get {
  url: {{baseUrl}}/notifications/unread-count
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{notificationUserAccessToken}}
}

tests {
  test("get unread count returns 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
