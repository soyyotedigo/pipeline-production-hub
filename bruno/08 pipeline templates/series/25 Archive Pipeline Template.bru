meta {
  name: 25 Archive Pipeline Template
  type: http
  seq: 25
}

post {
  url: {{baseUrl}}/pipeline-templates/{{templateId}}/archive
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{accessToken}}
}

tests {
  test("archive pipeline template returns 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
