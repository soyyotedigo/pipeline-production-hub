meta {
  name: 11 List Asset A Shots
  type: http
  seq: 11
}

get {
  url: {{baseUrl}}/assets/{{assetAId}}/shots
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{accessToken}}
}

tests {
  test("list asset shots returns 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
