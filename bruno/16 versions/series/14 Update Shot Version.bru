meta {
  name: 14 Update Shot Version
  type: http
  seq: 14
}

patch {
  url: {{baseUrl}}/versions/{{shotVersionId}}
  body: json
  auth: none
}

headers {
  content-type: application/json
  Authorization: Bearer {{accessToken}}
}

body:json {
  {
    "description": "Updated shot version from Bruno versions collection."
  }
}

tests {
  test("update shot version returns 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
