meta {
  name: 18 List Project Versions
  type: http
  seq: 18
}

get {
  url: {{baseUrl}}/projects/{{projectId}}/versions
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{accessToken}}
}

tests {
  test("list project versions returns 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
