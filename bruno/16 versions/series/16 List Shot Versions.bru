meta {
  name: 16 List Shot Versions
  type: http
  seq: 16
}

get {
  url: {{baseUrl}}/shots/{{shotId}}/versions
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{accessToken}}
}

tests {
  test("list shot versions returns 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
